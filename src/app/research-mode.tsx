import { useState } from 'react';import*as FileSystem from'expo-file-system/legacy';import{router}from'expo-router';import*as Sharing from'expo-sharing';import{Alert,Pressable,StyleSheet,TextInput,View}from'react-native';import{AppText}from'@/components/AppText';import{FilterChip}from'@/components/FilterChip';import{GlassCard}from'@/components/GlassCard';import{PrimaryButton}from'@/components/PrimaryButton';import{TabScreen}from'@/components/TabScreen';import{completeResearchSession,exportResearchCsv,exportResearchJson,recordResearchEvent,startResearchSession}from'@/database/repositories/researchRepository';import{useTheme}from'@/theme/ThemeProvider';import{radii,spacing}from'@/theme/tokens';
export default function ResearchModeScreen(){const{colors}=useTheme();const[session,setSession]=useState<{id:number;uuid:string;startedAt:string}|null>(null);const[idea,setIdea]=useState(8);const[ease,setEase]=useState(8);const[useful,setUseful]=useState(true);const[wouldUse,setWouldUse]=useState(true);const[unclear,setUnclear]=useState('');const[best,setBest]=useState('Закрыть остаток');const begin=async()=>{const next=await startResearchSession();setSession(next);await recordResearchEvent(next.id,'scenario_started','research',{consent:true});};const finish=async()=>{if(!session)return;await recordResearchEvent(session.id,'scenario_completed','research',{survey:true});await completeResearchSession(session.id,{idea,ease,useful,wouldUse,unclear,best});Alert.alert('Спасибо','Ответы сохранены анонимно и только на этом устройстве.');setSession(null);};const exportFiles=async()=>{const[csv,json]=await Promise.all([exportResearchCsv(),exportResearchJson()]);for(const[type,content,mime]of[['csv',csv,'text/csv'],['json',JSON.stringify(json,null,2),'application/json']]as const){const uri=`${FileSystem.cacheDirectory}tochka-rosta-research.${type}`;await FileSystem.writeAsStringAsync(uri,content);if(await Sharing.isAvailableAsync())await Sharing.shareAsync(uri,{mimeType:mime});}};const log=async(type:string)=>{if(session)await recordResearchEvent(session.id,type,'guided_research',{anonymous:true});};return <TabScreen title="Исследовательский режим" subtitle="Анонимное локальное тестирование" headerRight={<Pressable style={[styles.close,{backgroundColor:colors.surface}]} onPress={()=>router.back()}><AppText>×</AppText></Pressable>}>{!session?<><GlassCard variant="accent"><AppText variant="heading">Перед началом</AppText><AppText tone="secondary">Режим фиксирует время, переходы, способы добавления и ошибки сценария. Имя, фотографии, параметры профиля и точные пищевые данные не записываются.</AppText></GlassCard><PrimaryButton label="Начать тестирование" onPress={begin}/><PrimaryButton label="Экспортировать CSV и JSON" secondary onPress={exportFiles}/></>:<><GlassCard variant="compact"><AppText variant="caption" tone="green">ТЕСТ ИДЁТ</AppText><AppText variant="heading">Отметки сценария</AppText><AppText tone="secondary">Нажимай после выполненного действия. Записывается только тип события.</AppText><View style={styles.ratings}>{[['food_search','Использован поиск'],['qr_used','Использован QR'],['remainder_used','Закрыт остаток'],['back_navigation','Возврат назад'],['user_error','Возникла ошибка'],['flow_understood','Поток понятен']].map(([type,label])=><Pressable key={type} style={[styles.event,{borderColor:colors.glassBorder}]} onPress={()=>{void log(type);}}><AppText variant="caption" tone="green">＋ {label}</AppText></Pressable>)}</View></GlassCard><Rating label="Насколько понятна идея приложения?" value={idea} onChange={setIdea}/><Rating label="Насколько легко добавить продукт?" value={ease} onChange={setEase}/><YesNo label="Полезна ли функция «Закрыть остаток»?" value={useful} onChange={setUseful}/><YesNo label="Стали бы вы пользоваться приложением?" value={wouldUse} onChange={setWouldUse}/><Field label="Что было непонятно?" value={unclear} onChange={setUnclear}/><Field label="Какая функция самая полезная?" value={best} onChange={setBest}/><PrimaryButton label="Завершить и сохранить" onPress={finish}/></>}</TabScreen>;}
function Rating({label,value,onChange}:{label:string;value:number;onChange:(value:number)=>void}){return <GlassCard variant="compact"><AppText style={styles.bold}>{label}</AppText><View style={styles.ratings}>{Array.from({length:10},(_,i)=>i+1).map((item)=><FilterChip key={item} label={String(item)} selected={value===item} onPress={()=>onChange(item)}/>)}</View></GlassCard>;}function YesNo({label,value,onChange}:{label:string;value:boolean;onChange:(value:boolean)=>void}){return <GlassCard variant="compact"><AppText style={styles.bold}>{label}</AppText><View style={styles.ratings}><FilterChip label="Да" selected={value} onPress={()=>onChange(true)}/><FilterChip label="Нет" selected={!value} onPress={()=>onChange(false)}/></View></GlassCard>;}function Field({label,value,onChange}:{label:string;value:string;onChange:(value:string)=>void}){const{colors}=useTheme();return <GlassCard variant="compact"><AppText style={styles.bold}>{label}</AppText><TextInput value={value} onChangeText={onChange} multiline style={[styles.input,{color:colors.textPrimary,borderColor:colors.glassBorder,backgroundColor:colors.surface}]}/></GlassCard>;}
const styles=StyleSheet.create({close:{width:42,height:42,borderRadius:21,alignItems:'center',justifyContent:'center'},ratings:{flexDirection:'row',flexWrap:'wrap',gap:spacing.xs,marginTop:spacing.sm},event:{minHeight:42,borderWidth:1,borderRadius:radii.pill,paddingHorizontal:spacing.sm,alignItems:'center',justifyContent:'center'},bold:{fontWeight:'700'},input:{minHeight:72,borderWidth:1,borderRadius:radii.md,padding:spacing.md,marginTop:spacing.sm,textAlignVertical:'top'}});
