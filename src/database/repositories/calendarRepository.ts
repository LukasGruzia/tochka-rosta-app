import type { CalendarDayStatus } from '@/types/domain';
import { getLocalDateKey } from '@/utils/date';
import { getDatabase } from '../database';
function monthBounds(monthKey:string){const [year,month]=monthKey.split('-').map(Number);const start=getLocalDateKey(new Date(year,month-1,1,12));const end=getLocalDateKey(new Date(year,month,0,12));return{start,end};}
export async function loadCalendarMonth(monthKey:string):Promise<CalendarDayStatus[]>{const db=await getDatabase();const{start,end}=monthBounds(monthKey);const [days,pauses]=await Promise.all([db.getAllAsync<{date:string;is_completed:number;entry_count:number}>(`SELECT d.date,d.is_completed,COUNT(e.id) AS entry_count FROM diary_days d LEFT JOIN diary_entries e ON e.diary_day_id=d.id AND e.deleted_at IS NULL WHERE d.date BETWEEN ? AND ? GROUP BY d.id ORDER BY d.date`,start,end),db.getAllAsync<{date:string}>('SELECT date FROM flow_pauses WHERE date BETWEEN ? AND ?',start,end)]);const pauseSet=new Set(pauses.map((row)=>row.date));const today=getLocalDateKey();return days.map((row)=>{const entryCount=row.entry_count??0;const isCompleted=row.is_completed===1;const isPaused=pauseSet.has(row.date);const kind:CalendarDayStatus['kind']=isCompleted?'completed':entryCount?'filled':row.date<today?'missed':row.date>today?'future':'empty';return{date:row.date,entryCount,isCompleted,isPaused,isMilestone:false,kind};});}
export async function loadWeeklyFlowSummary(start:string,end:string){const db=await getDatabase();const row=await db.getFirstAsync<{closed_days:number;entry_count:number;avg_accuracy:number;avg_protein:number}>(`SELECT SUM(CASE WHEN d.is_completed=1 THEN 1 ELSE 0 END) AS closed_days,COUNT(e.id) AS entry_count,AVG(CASE WHEN d.target_calories>0 AND d.consumed_calories>0 THEN MAX(0,100-ABS(d.consumed_calories-d.target_calories)*100.0/d.target_calories) END) AS avg_accuracy,AVG(CASE WHEN d.consumed_calories>0 THEN d.consumed_protein_g END) AS avg_protein FROM diary_days d LEFT JOIN diary_entries e ON e.diary_day_id=d.id AND e.deleted_at IS NULL WHERE d.date BETWEEN ? AND ?`,start,end);return{closedDays:row?.closed_days??0,entryCount:row?.entry_count??0,averageAccuracy:row?.avg_accuracy??0,averageProtein:row?.avg_protein??0};}
